"use client";
import type { AppData } from "@/lib/types";

/**
 * قسم المشاريع — فارغ حالياً.
 *
 * الخيار يبقى موجوداً في قائمة التنقل، لكن الصفحة نفسها خالية
 * بانتظار التصميم الجديد.
 */
export default function ProjectsSection(_props: { data: AppData }) {
  return <div className="min-h-[60vh] w-full" />;
}
